describe('Authentication service', function () {

    beforeEach(module('satest'));

    it('should ...', inject(function (Authentication) {

        //expect(authenticationService.doSomething()).toEqual('something');

    }));

});