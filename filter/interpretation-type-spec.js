describe('interpretationType', function() {

	beforeEach(module('satest'));

	it('should ...', inject(function($filter) {

        var filter = $filter('interpretationType');

		expect(filter('input')).toEqual('output');

	}));

});